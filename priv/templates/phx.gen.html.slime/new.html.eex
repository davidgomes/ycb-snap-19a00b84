h2 New <%= schema.human_singular %>

= render "form.html", Map.merge(assigns, %{action: <%= schema.route_helper %>_path(@conn, :create)})

= link "Back", to: <%= schema.route_helper %>_path(@conn, :index)
