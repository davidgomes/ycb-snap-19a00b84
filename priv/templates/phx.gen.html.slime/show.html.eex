h2 Show <%= schema.human_singular %>

ul
<%= for {k, _} <- schema.attrs do %>  li
    strong <%= Phoenix.Naming.humanize(Atom.to_string(k)) %>:&nbsp;
    = @<%= schema.singular %>.<%= k %>
<% end %>
span= link "Edit", to: <%= schema.route_helper %>_path(@conn, :edit, @<%= schema.singular %>)
| &nbsp;
span= link "Back", to: <%= schema.route_helper %>_path(@conn, :index)
