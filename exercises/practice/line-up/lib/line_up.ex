defmodule LineUp do
  @doc """
  Given a name and a number, return a ticket sentence with the number as an ordinal numeral.
  """
  @spec format(name :: String.t(), number :: pos_integer()) :: String.t()
  def format(name, number) do
  end
end
