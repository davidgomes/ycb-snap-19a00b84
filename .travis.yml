language: elixir
elixir:
  - 1.9
otp_release:
  - 22.0
services:
  - postgresql
addons:
  postgresql: "10"
env:
  - MIX_ENV=test
before_script:
  - psql -c 'create database gen_queue_oban_test;' -U postgres
script:
  - mix format --check-formatted
  - mix test
