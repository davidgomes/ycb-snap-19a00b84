language: elixir

elixir:
  - 1.9.4

otp_release:
  - 22.1

services:
  - postgresql

addons:
  postgresql: "10"

env:
  global:
    - MIX_ENV=test

script:
  - mix format --check-formatted
  - mix test
