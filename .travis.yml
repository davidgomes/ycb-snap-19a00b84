language: elixir
elixir:
  - 1.8
  - 1.9
otp_release:
  - 21.3
  - 22.0
addons:
  postgresql: "9.6"
services:
  - postgresql
env:
  - MIX_ENV=test POSTGRES_USER=postgres POSTGRES_PASSWORD=
before_script:
  - mix format --check-formatted
script:
  - mix test
